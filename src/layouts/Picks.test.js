import React from 'react';
import { shallow } from 'enzyme';
import Picks from './Picks';

describe('Picks', () => {
    it('should render correctly', () => {
        const component = shallow(<Picks />);
    });
});
